const kcbConfig = { environment: process.env.KCB_ENV || 'sandbox', consumerKey: process.env.KCB_CONSUMER_KEY, consumerSecret: process.env.KCB_CONSUMER_SECRET, tokenUrl: process.env.KCB_TOKEN_URL || 'https://accounts.buni.kcbgroup.com/oauth2/token', ipnBaseUrl: process.env.KCB_IPN_BASE_URL || 'https://uat.buni.kcbgroup.com/ipn/1.0.0', paybill: process.env.KCB_PAYBILL, webhookSecret: process.env.KCB_WEBHOOK_SECRET, timeout: 30000, retryAttempts: 3, retryDelay: 1000 }; const validateConfig = () => { const required = ['consumerKey', 'consumerSecret', 'tokenUrl']; const missing = required.filter((key) => !kcbConfig[key]); if (missing.length > 0) { throw new Error('[KCB Config] Missing required env vars: ' + missing.join(', ')); } }; module.exports = { kcbConfig, validateConfig };
