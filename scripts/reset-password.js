const pool = require('./src/db/connection'); const bcrypt = require('bcrypt'); bcrypt.hash('Admin2026!', 10).then(hash => pool.query('UPDATE users SET password = ' + String.fromCharCode(36) + '1 WHERE email = ' + String.fromCharCode(36) + '2', [hash, 'john@example.com'])).then(() => { console.log('Password reset!'); process.exit(); }).catch(e => { console.error(e.message); process.exit(); });
