const pool = require('../db/connection'); const getByEmail = async (email) => { const result = await pool.query('SELECT * FROM users WHERE email = ' + String.fromCharCode(36) + '1', [email]); return result.rows[0]; }; const create = async (user) => { const { full_name, email, password_hash, role } = user; const result = await pool.query('INSERT INTO users (name, email, password, role) VALUES (' + String.fromCharCode(36) + '1,' + String.fromCharCode(36) + '2,' + String.fromCharCode(36) + '3,' + String.fromCharCode(36) + '4) RETURNING *', [full_name, email, password_hash, role]); return result.rows[0]; }; module.exports = { getByEmail, create };
